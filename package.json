{
  "name": "algorithm",
  "version": "1.0.0",
  "description": "You can find the algorithm explanation in every file.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.8.6",
    "typescript": "^5.2.2"
  }
}
